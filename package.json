{
	"name": "psalmsandhymns-website",
	"private": true,
	"engines": {
		"node": ">=20"
	},
	"dependencies": {
		"@vercel/node": "^3.2.0",
		"nodemailer": "^6.9.16"
	},
	"devDependencies": {
		"@types/nodemailer": "^6.4.17"
	},
	"scripts": {
		"dev": "npm run dev --prefix web",
		"build": "npm run build --prefix web",
		"preview": "npm run preview --prefix web",
		"test": "npm run test --prefix web"
	}
}
