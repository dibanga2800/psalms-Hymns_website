{
	"installCommand": "npm install && npm install --prefix web && npm install --prefix sanity",
	"buildCommand": "npm run build --prefix web",
	"outputDirectory": "web/dist",
	"framework": null,
	"rewrites": [
		{ "source": "/((?!api/).*)", "destination": "/index.html" }
	]
}
